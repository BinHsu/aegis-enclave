# Local development environment variables.
# Copy to .env (gitignored) and adjust. Production uses Secrets Manager
# (see ADR-0016) — never commit real values.

POSTGRES_USER=primes_app
POSTGRES_PASSWORD=changeme_local_dev_only
POSTGRES_DB=primes
POSTGRES_HOST=db
POSTGRES_PORT=5432

LOG_LEVEL=INFO
APP_VERSION=0.1.0
